version: '3'

services:
  dashboard:
    build: 
      context: /
    ports:
      - 5000
    
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - dashboard
    ports:
      - 80:80
